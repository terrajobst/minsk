<Project>

  <PropertyGroup Condition="'$(MSBuildProjectExtension)' == '.msproj'">
    <LanguageTargets>$([MSBuild]::NormalizePath('$(MSBuildThisFileDirectory)', '..\Core\Minsk.Core.targets'))</LanguageTargets>
  </PropertyGroup>

  <Import Sdk="Microsoft.NET.Sdk" Project="Sdk.targets"/>

</Project>